<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.eappcat.starter.mapper.UserMapper">
    <select id="findAll" resultType="com.eappcat.starter.entity.User">
        select * from tbl_user
    </select>
    <select id="findAllByPage" resultType="com.eappcat.starter.entity.User">
        select * from tbl_user
    </select>
</mapper>